import type { LocalSettingsType } from "@/types/user";

export const initLocalSettings: LocalSettingsType = {
  syncTheme: true,
  theme: "TODOIST",
  useSystemTheme: false,
};

export const maxNameLength = 255;
